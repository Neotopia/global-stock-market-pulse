repos:
  - repo: local
    hooks:
      - id: detect-secrets
        name: detect-secrets
        entry: detect-secrets-hook
        language: system
        args: ['--baseline', '.secrets.baseline']
        exclude: package-lock\.yml$
